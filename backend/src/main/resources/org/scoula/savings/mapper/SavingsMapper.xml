<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.savings.mapper.SavingsMapper">

    <!-- 1. 적금 상품 저장 (SavingsProductVO 매핑) -->
    <insert id="upsertProduct" parameterType="org.scoula.savings.domain.SavingsProductVO"
            useGeneratedKeys="true" keyProperty="productId">
        INSERT INTO SAVINGS_PRODUCT (
            company_code, product_code, product_name, product_type,
            min_amount, max_amount, feature, target, interest_pay_type,
            detail_url, status
        ) VALUES (
                     #{companyCode}, #{productCode}, #{productName}, #{productType},
                     #{minAmount}, #{maxAmount}, #{feature}, #{target}, #{interestPayType},
                     #{detailUrl}, #{status}
                 )
        ON DUPLICATE KEY UPDATE
                             product_name = VALUES(product_name),
                             product_type = VALUES(product_type),
                             max_amount = VALUES(max_amount),
                             feature = VALUES(feature),
                             target = VALUES(target),
                             interest_pay_type = VALUES(interest_pay_type),
                             status = VALUES(status),
                             product_id = LAST_INSERT_ID(product_id)
    </insert>

    <!-- 2. 해당 상품의 기존 금리 삭제 -->
    <delete id="deleteRatesByProductId" parameterType="long">
        DELETE FROM SAVINGS_RATE WHERE product_id = #{productId}
    </delete>

    <!-- 3. 적금 금리 저장 (SavingsRateVO 매핑) -->
    <insert id="insertRate" parameterType="org.scoula.savings.domain.SavingsRateVO">
        INSERT INTO SAVINGS_RATE (
            product_id, save_term, min_rate, max_rate, start_date, end_date
        ) VALUES (
                     #{productId}, #{saveTerm}, #{minRate}, #{maxRate}, #{startDate}, #{endDate}
                 )
    </insert>

    <!-- 추천 적금 목록 조회 쿼리 (가입기간 + 월 납입 한도 조건 필터링) -->
    <select id="selectRecommendedProducts" resultType="org.scoula.savings.dto.SavingsRecommendDTO">
        SELECT
        p.product_id   AS productId,
        p.product_name AS productName,
        p.product_type AS productType,
        p.company_code AS companyCode,
        r.max_rate     AS maxRate
        FROM SAVINGS_PRODUCT p
        JOIN SAVINGS_RATE r ON p.product_id = r.product_id
        WHERE p.status = 10
        AND r.save_term = #{saveTerm}
        <!-- 1. 사용자의 월 납입액이 DB에 보정된 최소 납입액 이상인지 확인 -->
        AND #{monthlyAmount} >= p.min_amount
        <!-- 2. 최대 납입액이 0/NULL(제한없음)이거나, 최대 납입액 한도 이하인 상품만 조회 -->
        AND (p.max_amount IS NULL OR p.max_amount = 0 OR p.max_amount >= #{monthlyAmount})
        ORDER BY r.max_rate DESC
    </select>

    <!-- 상품 기본 정보 조회 (SavingsProductVO 반환) -->
    <select id="selectProductById" parameterType="long" resultType="org.scoula.savings.domain.SavingsProductVO">
        SELECT
            product_id AS productId,
            company_code AS companyCode,
            product_code AS productCode,
            product_name AS productName,
            product_type AS productType,
            min_amount AS minAmount,
            max_amount AS maxAmount,
            feature,
            target,
            interest_pay_type AS interestPayType,
            detail_url AS detailUrl,
            status
        FROM SAVINGS_PRODUCT
        WHERE product_id = #{productId} AND status = 10
    </select>

    <!-- 기간별 금리 목록 조회 (SavingsRateVO 반환) -->
    <select id="selectRatesByProductId" parameterType="long" resultType="org.scoula.savings.domain.SavingsRateVO">
        SELECT
            rate_id AS rateId,
            product_id AS productId,
            save_term AS saveTerm,
            min_rate AS minRate,
            max_rate AS maxRate,
            start_date AS startDate,
            end_date AS endDate
        FROM SAVINGS_RATE
        WHERE product_id = #{productId}
        ORDER BY save_term ASC
    </select>


    <!--
          사용자 적금 가입 정보 INSERT
          useGeneratedKeys="true" : DB에서 자동 생성된 PK(AUTO_INCREMENT)를 가져옴
          keyProperty="subscriptionId" : 삽입 후 VO의 subscriptionId 필드에 해당 PK 값을 채워줌
        -->
    <insert id="insertSubscription" parameterType="org.scoula.savings.domain.SubscriptionVO"
            useGeneratedKeys="true" keyProperty="subscriptionId">
        INSERT INTO subscription (
            deposit_id,
            product_id,
            status,
            start_date,
            end_date,
            account_no,
            applied_rate,
            monthly_amount,
            user_save_term,
            user_save_type,
            payment_day,
            created_at
        ) VALUES (
                     #{depositId},
                     #{productId},
                     #{status},
                     #{startDate},
                     #{endDate},
                     #{accountNo},
                     #{appliedRate},
                     #{monthlyAmount},
                     #{userSaveTerm},
                     #{userSaveType},
                     #{paymentDay},
                     NOW()
                 )
    </insert>

    <!-- 특정 상품의 특정 가입기간(개월)에 해당하는 단건 금리 조회 -->
    <select id="selectRateByTerm" resultType="org.scoula.savings.domain.SavingsRateVO">
        SELECT
            rate_id AS rateId,
            product_id AS productId,
            save_term AS saveTerm,
            min_rate AS minRate,
            max_rate AS maxRate,
            start_date AS startDate,
            end_date AS endDate
        FROM SAVINGS_RATE
        WHERE product_id = #{productId}
          AND save_term = #{saveTerm}
        LIMIT 1
    </select>

    <!-- 납입 내역(Payment) INSERT -->
    <insert id="insertPayment" parameterType="org.scoula.savings.domain.PaymentVO"
            useGeneratedKeys="true" keyProperty="paymentId">
        INSERT INTO PAYMENT (
            subscription_id,
            round_no,
            amount,
            paid_at,
            created_at
        ) VALUES (
                     #{subscriptionId},
                     #{roundNo},
                     #{amount},
                     #{paidAt},
                     NOW()
                 )
    </insert>

    <!-- 총 납입 원금 합산 -->
    <select id="selectTotalPrincipal" resultType="java.lang.Long">
        SELECT SUM(amount)
        FROM PAYMENT
        WHERE subscription_id = #{subscriptionId}
    </select>

    <!-- 하단 차트용 월별 납입 데이터 추출 (연도 포함 및 정렬된 최종 버전) -->
    <select id="selectMonthlyPayments" resultType="org.scoula.savings.dto.MonthlyPaymentDTO">
        SELECT
            CAST(SUBSTRING(CAST(paid_at AS CHAR), 1, 4) AS UNSIGNED) AS payYear,
            CAST(SUBSTRING(CAST(paid_at AS CHAR), 5, 2) AS UNSIGNED) AS payMonth,
            SUM(amount) AS amount
        FROM PAYMENT
        WHERE subscription_id = #{subscriptionId}
        GROUP BY payYear, payMonth
        ORDER BY payYear ASC, payMonth ASC
    </select>

    <!-- 마지막 납입일(가장 최근 결제일) 조회 -->
    <select id="selectLastPaymentDate" resultType="java.lang.Integer">
        SELECT MAX(paid_at)
        FROM PAYMENT
        WHERE subscription_id = #{subscriptionId}
    </select>

    <!-- 가입 내역 + 상품 정보 조인 조회 (상품명 매핑용) -->
    <select id="selectSubscriptionWithProduct" resultType="org.scoula.savings.domain.SubscriptionVO">
        SELECT
            s.*,
            p.product_name AS productName,
            p.company_code AS companyCode
        FROM SUBSCRIPTION s
                 INNER JOIN SAVINGS_PRODUCT p ON s.product_id = p.product_id
        WHERE s.subscription_id = #{subscriptionId}
    </select>

    <!-- 현재까지 납입한 총 회차(건수) 조회 -->
    <select id="selectPaidRounds" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM PAYMENT
        WHERE subscription_id = #{subscriptionId}
    </select>

</mapper>