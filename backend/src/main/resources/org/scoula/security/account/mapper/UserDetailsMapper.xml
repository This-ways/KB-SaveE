<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.scoula.security.account.mapper.UserDetailsMapper">
    <resultMap id="userMap" type="org.scoula.user.domain.UserVO">
        <id property="userId" column="user_id" />
        <result property="loginId" column="login_id" />
        <result property="password" column="password" />
        <result property="userName" column="user_name" />
        <result property="birthDate" column="birth_date" />
        <result property="pushEnabled" column="push_enabled" />
        <result property="mydataConnected" column="mydata_connected" />
        <result property="status" column="status" />
        <result property="tokenVersion" column="token_version" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <select id="get" resultMap="userMap">
        SELECT user_id, login_id, password, user_name, birth_date, push_enabled, mydata_connected, status, token_version, created_at, updated_at
        FROM `user`
        WHERE login_id = #{loginId}
          AND status = 10
    </select>
</mapper>